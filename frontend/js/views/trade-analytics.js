/**
 * trade-analytics.js — Trading analytics and reports (Phase 5).
 */

export async function render(container) {
    container.innerHTML = `
        <div class="page-header">
            <h1>Analytics</h1>
            <p>Win rate, profit factor, R-multiples, equity curve — coming in Phase 5.</p>
        </div>
        <p class="text-muted">Deep analysis of your trading performance across all accounts and strategies.</p>
    `;
}
