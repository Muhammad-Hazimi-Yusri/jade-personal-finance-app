/**
 * budgets.js — Budget management view (Phase 3).
 */

export async function render(container) {
    container.innerHTML = `
        <div class="page-header">
            <h1>Budgets</h1>
            <p>Monthly and weekly budget tracking — coming in Phase 3.</p>
        </div>
        <p class="text-muted">Set spending limits per category and track your progress here.</p>
    `;
}
