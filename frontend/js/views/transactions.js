/**
 * transactions.js — Transactions list view (Phase 1.6).
 * Paginated, sortable, filterable transaction list.
 */

export async function render(container) {
    container.innerHTML = `
        <div class="page-header">
            <h1>Transactions</h1>
            <p>Transaction list with filters and pagination — coming in Phase 1.6.</p>
        </div>
        <p class="text-muted">Your transactions will appear here once Phase 1.6 is complete.</p>
    `;
}
