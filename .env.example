# Litestream S3/R2 backup credentials (optional — only needed for 'litestream' service)
S3_ACCESS_KEY=your_access_key_id_here
S3_SECRET_KEY=your_secret_access_key_here

# Demo mode — set to "true" to show the demo banner and X-Demo-Mode response header
DEMO_MODE=false
